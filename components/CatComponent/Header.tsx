import { Text, View } from "react-native";

const Header = () => {
  return (
    <View className="pt-[100px] px-5 pb-[50px] bg-[#ddebf8] flex-row justify-center">
      <Text className="text-3xl font-bold text-[#359ce1]">WHERE IS CAT?</Text>
    </View>
  );
};

export default Header;
